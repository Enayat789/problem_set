// Create a function that will convert from Celsius to Fahrenheit

// function sumNumbers(celsius) {
//   //   let celsius = 26;
//   let fahrengeit;
//   fahrengeit = (celsius * 9) / 5 + 32 + " F ";

//   return fahrengeit;
// }

function sumNumbers(fahrengeit) {
  let celsius;
  celsius = ((fahrengeit - 32) * 5) / 9 + " C ";

  return celsius;
}

const result = sumNumbers(26);
console.log(result);
